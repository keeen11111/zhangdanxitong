# 项目当前状态

> 最后核对：2026-09-01。本文以代码、自动化校验和 Git 历史为准；`tasks/todo.md` 是后续工作的唯一任务入口。

## 一句话状态

AutoPayroll-Pro 已是可构建、可测试的内部薪资 Excel Agent Web MVP：核心月度处理闭环、逐人对话和人工处理回导已具备；通用财务首期已打通“通用总表 + 来源文件 → 安全映射 → 草稿核对 → Agent/人工确认 → 正式 Excel 下载”的闭环。

## 本次验证结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 后端与核心引擎测试 | 通过 | `.venv-codex\\Scripts\\python.exe -m pytest -q`：472 passed |
| 前端逻辑测试 | 待补跑 | 当前环境没有 `tsx` 测试启动器；测试文件已保留，不能以未执行结果代替通过 |
| 前端静态检查 | 通过 | `tsc --noEmit` 通过；`next lint` 无 ESLint 错误或警告 |
| 前端生产构建 | 通过 | Next.js 生产构建、类型检查和静态页生成完成 |
| 项目文档 | 已统一入口 | 状态、技术栈、流程图与任务台账互相链接，可按优先级推进。 |

上述检查仅证明当前代码可测试、可构建；不等同于生产部署验收，也不等同于真实薪资数据的本轮业务验收。

## 已可用的业务能力

- 用户登录、租户身份校验、项目和处理月份管理。
- 上传工资主模板及多份来源 Excel，识别并解析人员、薪资、考勤、社保公积金和个税数据。
- 六阶段核算主线：原始库、来源表、差异计算、审核、最终库、Excel 导出。
- 导出前执行 Blocker / Warning 校验；存在 Blocker 时拒绝导出。
- 保留工资模板的公式、格式和业务 Sheet；插入员工时扩展必要公式。
- 将总表与待人工处理清单分开导出；可将填写后的人工处理表回传并重新生成结果。
- 对部分同义 Sheet 与来源 Sheet 做语义判断，避免简单依赖文件名。

## 当前工作分层

| 层级 | 状态 | 说明 |
| --- | --- | --- |
| MVP 月度核算闭环 | 已完成并验证 | 可用于内部演示和继续业务验收。 |
| 人工处理交付闭环 | 已完成并验证 | 2026-08-24 的最新提交实现了人工决策回导和已处理事项过滤。 |
| 通用语义 Sheet 归并 | 已有首期安全实现 | 不依赖固定公司名、Sheet 名或列位；只有稳定业务主键和唯一映射才写入，未知记录、冲突与公式目标进入人工事项。 |
| 架构收敛与数据层 | 未完成 | 旧路由仍并行注册，仍以 SQLite 和 session JSON 为默认实现。 |
| 安全、审计与可观测性 | 部分完成 | 已有 CORS、上传校验和健康检查；未形成完整审计表、结构化日志和正式安全验收。 |
| 前端产品化 | 部分完成 | 已有可构建的 Next.js 前端；左侧已加入数据驱动的“待处理事项”，汇总人工复核、阻断与待发布批次。历史计划所写的 Ant Design 重写与当前 Radix/Tailwind 实现不一致，需单独决策。 |
| 部署与运维 | 未开始 | 尚无 Alembic、Dockerfile、docker-compose、Nginx 或 CI/CD 工作流。 |
| 文档驱动 Excel Agent | 首期完成并验证 | 已接入 OpenAI-compatible Responses API；模型只能通过白名单工具读取/建议，确定性执行器负责写入、公式保护、回滚和发布。手册或已提供的转写稿可编译为候选包，逐条审阅证据后启用；录音仅存档，不参与自动转写或规则编译。生效规则包会进入新运行上下文。 |
| 通用财务产品内核 | 第一阶段完成并验证 | 通用总表与来源文件已独立角色隔离；映射仅在有主题证据时自动写入，拦截来源/数组公式，并返回逐单元格审计记录。结果只作为草稿；无阻断项时仍需人工确认发布。发布后会生成含发布人、时间与 SHA-256 的独立版本档案，当前文件变化不会影响历史版本。浏览器冒烟与跨公司样本验收待继续。 |

## 主要事实与限制

1. 当前默认数据库是 SQLite；PostgreSQL 仅是已记录的目标架构，不是当前交付能力。
2. 流水线会话、部分审核和导出元数据仍写入本地 JSON 文件，因此不适合多实例或高并发生产环境。
3. `backend/main.py` 仍同时注册 `modules`、`payroll`、`entities` 与 `pipeline` 路由；`pipeline` 尚未成为唯一业务入口。
4. `tasks/todo.md` 的旧勾选状态曾滞后于代码。现在的任务状态以本文件和更新后的任务台账为准。
5. 真实薪资文件、上传文件、数据库和导出文件必须保持在本地或获批准的内网环境，不能提交到仓库。

## 当前优先级

1. 用两到三组跨公司脱敏样本做浏览器端验收：上传、规则包审阅、逐人 Agent 对话、草稿、人工事项、发布和正式下载全流程；不以任何一家公司结构写死规则。录音如需参与分析，由用户另行上传人工整理的转写稿。
2. 增加工作簿画像与映射解释，展示候选目标、业务主键、置信度和未自动处理原因。
3. 在数据库 schema 获确认后，将本地 JSON 版本档案升级为追加式审计表、对象存储保留策略和多人审批。
4. 以特征化测试为前提，将旧路由收敛至 `pipeline`，再迁移 PipelineSession 和审计数据。
5. 引入 PostgreSQL、Alembic、Docker Compose 和最低限度的 CI，形成可在内网部署的交付包。

## 相关文档

- [技术栈与架构](technology-stack.md)
- [月度处理流程](payroll-processing-flow.md)
- [任务总台账](../tasks/todo.md)
- [语义 Sheet 归并规格](semantic-sheet-integration-spec.md)
- [通用财务工作簿平台规格草案](universal-financial-workbook-spec.md)
- [正式财务账单版本留档](published-financial-version-archive.md)
